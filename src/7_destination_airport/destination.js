import React from "react";
import "/destination.css"

export default class Destination extends Component {

    constructor(props) {
        super(props);
        this.state= {
            initial_buttons:true
        }
    }

    render() {
        return(
            <div className={"Left Side"}>

            </div>
        );
    }
}